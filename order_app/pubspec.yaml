name: order_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  # TODO 0: Add objectbox dependency
  # TODO 0.1: Add objectbox_sync_flutter_libs dependency

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  # TODO 0: Add build_runner dependency
  # TODO 0.1: Add objectbox_generator dependency

flutter:
  uses-material-design: true
