name: chef_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  #TODO 0: Add objectbox and objectbox_sync_flutter_libs dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  #TODO 0.1: Add build_runner and objectbox_generator dependencies

flutter:
  uses-material-design: true

